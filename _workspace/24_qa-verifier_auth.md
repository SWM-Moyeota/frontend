# 인증 통합 QA 최종 리포트

작성: 2026-09-02 (1차: qa-verifier 에이전트 / 2차 잔여 항목: 리더 직접 수행)
검증 환경: emulator-5554 (Pixel_6 콜드부트), 백엔드 feature/userdomain local 프로필 (H2 인메모리)

## 집계: 통과 15 · 결함 3(전부 조치/보고됨) · 미검증 0

| # | 항목 | 결과 | 근거 |
|---|------|------|------|
| A1 | 앱 가입 → 자동 로그인 → 홈 | ✅ | POST /users 201 → login 200 연속, 보호 API에 Bearer 부착 확인 |
| A2 | 실계정 방 합류 → 21 대기 | ✅ | POST /matching/rooms/3/join 200, 서버 members에 토큰 주체(내부 id) 기록 |
| A3 | 정원 도달 → 자동 배차 → 25 + 기사 위치 폴링 | ✅ | 새 경로 GET /dispatch/rides/{partyId} 200 폴링, 기사 마커 렌더 (D-1 해소 확인) |
| A4-1 | 앱 재시작 → 로그인 유지 홈 직행 | ✅ | authqa_37 (재설치 후에도 유지 — authqa_40) |
| A4-2 | 로그아웃 → 확인 다이얼로그 → 로그인 화면 | ✅ | authqa_41~42, 만료 배너 없는 깨끗한 로그인 화면(문구 구분 정확) |
| A4-3 | **세션 만료 전이** (23번 미검증 항목) | ✅ | 서버 DB 초기화로 리프레시 무효화 → 앱이 감지, "세션이 만료됐어요" 배너와 함께 로그인 화면 (authqa_34) |
| B-1 | **토큰 만료 → 401 → reissue → 원요청 재시도** | ✅ | 앱: favorite-places → POST /auth/reissue 200 → 재시도 200, 화면 에러 무노출. 서버: 동시각 TOKEN_EXPIRED 로그 (15:08:11) |
| B-2 | 비보호 엔드포인트는 만료 토큰에도 정상 | ✅ | 범위 조회(비보호)는 만료 후에도 200 — @LoginUser 미적용 엔드포인트 특성, 정상 |
| C1 | 잘못된 비밀번호 | ✅ | 401 LOGIN_FAILED → 인라인 배너 |
| C2 | 중복 loginId 가입 | ✅(서버) | 409 LOGIN_ID_DUPLICATED — UI 표시는 C1과 동일 에러 배너 경로 공유 |
| C3 | airplane → 에러/재시도 UI → 복구 | ✅ | authqa_38(크래시 없음) → 39(복구) |
| D | 회귀: 홈 지도·검색·범위조회·마커·경로 미리보기·핀 조정·역지오코딩 | ✅ | 전부 Bearer 부착 상태로 200 |
| D-7 | 가입 생년월일·휴대폰 입력 커서 버그 | ✅ 수정·재검증 | VisualTransformation 교체 후 19950315→1995-03-15 정확 (authqa_43). PaymentAddScreen 동일 패턴 2곳도 선제 수정 |

## 신규 결함

- **D-8 (프론트) — 수정·재검증 완료**: 05 계정 유형 선택이 라우팅에 미반영이던 문제. MainNavGraph 분기 수정으로 학생→06, 직장인→08, 일반→09 3경로 실기 확인 (d8_04~06). 09 카피("저장하지 않아요")도 정정. 잔여 소소: 06 학교 이메일의 "이름과 학번은 저장하지 않아요" 문구도 이름 부분이 부정확(26 리포트 참조).
- **D-6 (백엔드, 기보고)**: body creatorId 방으로 인한 유령 멤버 — 방 생성 @LoginUser 전환(R-2)으로 근본 해결. 이번 검증에선 실계정 시드로 회피.
- **카피 잔재**: 09 부제 "실명과 성별은 … 저장하지 않아요" — 실제로는 서버에 저장됨. 문구 수정 필요(개인정보 고지 정확성).

## 백엔드 요청 잔여 (인증 관련)

leave 경로 더미 세그먼트(`/leave/{partyId}/0`) 제거, 방 생성 creatorId → @LoginUser(D-6/D-5), 방 상세에 isHost/myMemberId(방장 UI 판정), favorite POST 500(FavoritePlaceId 무인자 생성자), birthDate Instant → LocalDate, confirmCall 소유자 검증 TODO.

스크린샷: authqa_01~43. 상세 계약·구현: _workspace/21~23, 25 리포트.
